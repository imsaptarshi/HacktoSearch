import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      primary: "#090F19",
      secondary: "#FFA95A",
    },
  },
  fonts: {
    brand: "Rubik",
  },
});

export default theme;
